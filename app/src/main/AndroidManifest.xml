<?xml version="1.0" encoding="utf-8"?>
<manifest package="jp.gcreate.sample.linepaint"
          xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".DotActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HistoricalDotActivity">
        </activity>
        <activity android:name=".PathActivity">
        </activity>
        <activity android:name=".HistoricalPathActivity">
        </activity>
        <activity android:name=".BezierPathActivity">
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"
            >
        </activity>
    </application>

</manifest>
